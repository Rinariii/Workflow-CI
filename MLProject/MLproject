name: Clustering-Project
conda_env: conda.yaml
entry_points:
  main:
    parameters:
      data_path: {type: str, default: "../namadataset_preprocessing/loan_clean.csv"}
      output_dir: {type: str, default: "artifacts"}
    command: "python modelling.py --data_path {data_path} --output_dir {output_dir}"
